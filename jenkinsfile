pipeline {
  agent {label 'main'}

    stages {
      stage('install') {
        steps {
          sh 'npm install'
          }
        }

      stage ('test') {
        sh 'npm run test_app.py'
        }
    }
}

